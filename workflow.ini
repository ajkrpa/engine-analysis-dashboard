rocket_dashboard/
├── app.py              # The entry point: app = Dash(), callbacks that span pages
├── requirements.txt    # pip freeze > requirements.txt
├── data/               # Your test CSVs go here
├── pages/
│   ├── __init__.py
│   ├── overview.py     # Summary metrics, test selector
│   └── plots.py        # Time plots, FFT view
├── core/
│   ├── __init__.py
│   ├── data.py         # load_test_data(run_id) → DataFrame
│   ├── analysis.py     # compute_regression(df), find_peaks(df), fft_peaks(df)
│   └── utils.py        # helpers: detect_burn_window(df), format_units()
├── configs/
│   └── channels.yaml   # channel names, units, groups, limits
└── assets/
    └── style.css      # Hot fire dashboard theme
