# In Render: Build Command = `true` (or empty) — not `pip install` (this is a static site).
# "Use render.yaml" in the Render dashboard to apply these values.

services:
  - type: web
    name: rocket-dashboard-webapp
    runtime: static
    buildCommand: "true"
    staticPublishPath: ./webapp
